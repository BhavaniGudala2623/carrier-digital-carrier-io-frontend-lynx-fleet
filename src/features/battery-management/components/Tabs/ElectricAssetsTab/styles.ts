import { makeStyles } from '@mui/styles';

export const electricAssetsTabStyles = makeStyles(() => ({
  root: {
    backgroundColor: 'background.desktop',
    overflowY: 'hidden',
    display: 'flex',
    paddingBottom: 1,
    position: 'relative',
    height: '100%',
    width: '100%',
    flexDirection: 'column',
  },
}));
